# 日序 1.5.1 · 使用说明

## 安装

双击 `日序-完整安装包.exe` 即可。这个文件已经包含程序、图标和你选的星空背景原图，安装时不需要联网下载图片，也不需要携带单独图片文件。安装界面可以选择开机自启、桌面快捷方式、安装后打开。

首次使用会启用星空背景、深色浅字、16 号计划字号和横向窗口。覆盖安装保留原有计划、子任务、背景及外观选择。安装包不包含私人计划。若要把计划带到另一台电脑，请在设置中导出备份，到新电脑导入。

`Daynest.exe` 是直接运行的程序；`日序-完整安装包.exe` 才是安装器。通常只需保留安装器即可。当前已安装版本可以继续使用，无需为打包而重装。

## 使用

- 窗口底部“写下一个计划”创建今日必做、截止任务或长期计划。
- 父任务下方“＋ 添加子任务”新增步骤，Enter 连续添加；子任务独立勾选、编辑、删除，并显示进度。
- 任务“…”菜单提供编辑、归档、删除。左下角显示完成 / 归档总数及今日进度，点击可查看和恢复记录。
- 日期区切换日程，顶部非按钮区域可拖动。图钉置顶，圆角细条图标收起成小条，小条右侧图标展开。
- 小条显示时钟、日期星期、当前任务及截止或完成信息；展开收起使用碎片动画，日期切换使用星光动画。
- 设置中可更换背景、调整遮罩、窗口透明度、字体大小、开机自启、提醒、置顶与位置锁定。
- Ctrl + Alt + Shift + D 显示或隐藏窗口；关闭按钮收进托盘，托盘菜单可彻底退出。

## 保存与备份

计划修改后立即保存。主数据位于 `%LOCALAPPDATA%\Daynest`，独立备份位于 `%USERPROFILE%\Documents\日序备份`。保留 100 次内容快照与 30 个备份日。主文件缺失或异常时尝试恢复；卸载保留数据。设置中可打开数据与备份目录，并导出包含背景和子任务的备份文件。

新版数据使用 v3 格式，首次升级会保留 `before-v3-migration.json`。旧 v1/v2 备份可导入；旧程序不能读取新版数据。

## 本次打包验证

安装器释放的程序与构建文件一致；内置背景与原始 PNG 完全一致；新数据使用正确外观且没有私人计划；再次初始化和从备份恢复不会覆盖已有背景、计划及子任务。
